function calc(o){const c=TMS_CONFIG,s=new Date(o.OrderDateTime),d=new Date(o.DeliverDateTime);if(d<s)return{r24:'FAIL',geo:'FAIL',payload:'FAIL',reasons:['Delivery before OrderDateTime']};let start=s;if(s.getHours()>=c.sendCutoffHour&&d.toDateString()!==s.toDateString())start=new Date(d.getFullYear(),d.getMonth(),d.getDate());const duration=(d-start)/36e5,r24=duration<c.slaHours?'PASS':'FAIL',dist=o.distance_to_dropped<=c.geoRadiusMeter,gap=o.time_outlet_outlet>=c.minimumGapMinute,work=(d.getHours()*60+d.getMinutes())<=1200,geo=dist&&gap&&work?'PASS':'FAIL',payload=o.Assigned_Weight/o.TruckCapacityWeight<=c.payloadMaximumRatio?'PASS':'FAIL';const reasons=[];if(r24==='FAIL')reasons.push('24H SLA');if(!dist)reasons.push('Distance > 50m');if(!gap)reasons.push('Outlet gap < 2 min');if(!work)reasons.push('Delivered after working hours');if(payload==='FAIL')reasons.push('Payload > 150%');return{...o,r24,geo,payload,duration:duration.toFixed(1),reasons:reasons.join('; ')}}
